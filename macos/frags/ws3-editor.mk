# WS-3  Editor async read-back  (see ../PARALLEL-SUBAGENTS.md)
# VimbEditor.m is already listed in the shared Makefile's SRCS/TEST_SRCS blocks,
# so this stream adds NO new source files — the frag is intentionally empty to
# avoid duplicate-symbol links. The VimbEditor.m rewrite is a polish-only edit
# of an existing owned file.
